"use strict";
(() => {
    const enviarMision = (xmen) => {
        console.log(`Enviando a ${xmen.nombre} a la mision`);
    };
    const regresarAlCuartel = (xmen) => {
        console.log(`Enviando a ${xmen.nombre} a la mision`);
    };
    const wolve = {
        nombre: 'logan',
        edad: 50
    };
    enviarMision(wolve);
})();
